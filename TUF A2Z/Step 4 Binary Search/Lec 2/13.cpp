// Kth element of two sorted arrays 

#include <bits/stdc++.h>
using namespace std;

int main(){
  int n, m; cin >> n >> m;
  vector<int> nums1(n);
  vector<int> nums2(m);
  for (auto &i: nums1) cin >> i;
  for (auto &i: nums2) cin >> i;

  
}

