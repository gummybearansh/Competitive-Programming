// 1537B
#include <bits/stdc++.h>

#define ll long long

using namespace std;

void solve() {
  int n, m, i, j;
  cin >> n >> m >> i >> j;

  // start -> yoyo1 -> yoyo2 -> start
  // so maximise the distance between them
  cout << 1 << ' ' << m << ' ' << n << ' ' << 1 << '\n';
}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  int t;
  cin >> t;
  while (t--) {
    solve();
  }
  return 0;
}
